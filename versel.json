{
    "builds":[{
                "src":"mysite/wsgi.py",
                "use":"@vercel/python",
                "config":{"maxLambdaSize":"15md","runtime":"python3.10"}
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest":"mysite/wsgi.py"
        }
    ]
}